# 修改记录

## 2026-08-02 11:33 北京时间

### 修改目的

按评审确认后的方案，把项目对外名称统一为“电商铺货工具”，去掉旧产品名和“1688 自动铺货”主品牌文案；保留 1688 作为货源端口；新增拼多多、抖音商店、快手小店三个自动化铺货 API 目标。

### 主要修改

1. 前端名称与文案
   - 首页欢迎区改为“电商铺货工具”。
   - 登录页标题改为“电商铺货工具”。
   - PWA 标题、manifest、Service Worker 注释同步改名。
   - 选品页从“1688 选品”改为“货源商品/货源导入”表达。
   - 商品库空状态改为“去选品导入货源商品”。

2. 新增目标平台
   - 任务页新增目标平台：拼多多、抖音商店、快手小店。
   - 设置页新增三套配置区：
     - 拼多多：Client ID、Client Secret、Access Token、Mall ID、API 地址。
     - 抖音商店：App Key、App Secret、Access Token、Shop ID、API 地址。
     - 快手小店：App ID、App Secret、Access Token、Shop ID、API 地址。
   - 本地演示模式支持三类平台的任务执行记录和本地结果链接。

3. 后端 API 适配
   - 新增 `backend/app/push/marketplace.py`，作为多平台电商 API 铺货目标基类。
   - 新增 `backend/app/push/pdd.py`、`backend/app/push/douyin.py`、`backend/app/push/kuaishou.py`。
   - `PushTargetType` 新增 `pdd`、`douyin`、`kuaishou`。
   - 任务执行器注册并读取三个平台的配置。
   - 设置接口支持测试三个平台配置完整性。

4. 文档和静态产物
   - `README.md` 改名并新增多平台说明。
   - `API文档.md` 更新标题、任务目标枚举、设置类别和平台测试参数。
   - 重新构建前端，更新 `backend/static` 和 `docs`。
   - 删除旧构建残留的过期 JS 产物，防止页面继续引用旧代码。

### 保留内容

1. 1688 货源端口、搜索导入、offer 链接解析、页面解析兜底逻辑继续保留。
2. Shopify、通用 API、CSV 导出继续保留。
3. 旧数据表结构不删除，避免影响已有商品、任务和配置。

### 验证结果

1. `npm run build`：通过，已生成新的前端静态产物。
2. `python -m compileall backend/app`：通过，后端 Python 文件语法正常。
3. 文案检查：源码和当前静态产物中未再发现“铺货通”“1688 自动铺货”“1688 选品”等旧品牌主文案。

### 后续说明

拼多多、抖音商店、快手小店当前已经接入独立配置入口和后端目标适配器。真实上架需要填入对应平台的有效 API 地址、Token、店铺 ID 和应用凭证；配置缺失时，系统会返回明确提示，不会静默失败。

## 2026-08-02 11:42 北京时间

### 测试确认

本次使用生产构建页面 `http://localhost:4173/pds-tool/` 进行了实际流程测试，不是只检查样品模板。

### 测试步骤

1. 清空浏览器本地数据后重新打开页面。
2. 使用默认账号 `admin / admin123` 登录。
3. 首页显示“电商铺货工具”，仪表盘正常加载。
4. 设置页确认存在拼多多、抖音商店、快手小店三套配置入口。
5. 选品页批量导入 2 个货源 offer，页面返回“批量导入完成：成功 2 / 2”。
6. 商品库将 2 个商品从“已采集”转换为“已映射”。
7. 铺货页新建“拼多多测试铺货”任务，目标平台选择“拼多多”。
8. 执行任务后状态变为“已完成”，统计显示共 2 个、成功 2、失败 0。
9. 任务详情生成 2 条“拼多多成功”铺货记录，并带本地结果链接。
10. 执行日志显示“开始执行任务，筛选到 2 个商品”和“执行完成：成功 2，失败 0”。

### 测试结论

本地生产构建已跑通登录、货源导入、商品映射、拼多多任务创建、任务执行、铺货记录和执行日志。当前远程 GitHub Pages 因推送凭证未配置，还没有更新到这次新版本；远程页面更新需要完成 `git push origin main`。

## 2026-08-02 12:08 北京时间

### 平台开放能力调研

1. 拼多多开放平台提供官方 Java SDK，SDK 包含可直接导入的 jar 和源码包，网关地址为 `https://gw-api.pinduoduo.com/api/router`，商品发布主流程最终调用 `pdd.goods.add`。
2. 拼多多商品发布不是简单 JSON 入库，需要先获取店铺可发布叶子类目、上传平台图片、查询类目规则/属性/规格、设置价格库存和运费模板。
3. 抖店开放平台使用公共参数 `method/app_key/access_token/param_json/timestamp/v/sign/sign_method`，推荐 HMAC-SHA256，正式网关为 `https://openapi-fxg.jinritemai.com`。
4. 快手电商开放平台提供商品新增接口 `open.item.new`，线上推荐网关为 `https://openapi.kwaixiaodian.com`，系统参数包含 `appkey/timestamp/access_token/version/param/method/sign/signMethod`，支持 HMAC_SHA256 和 MD5，推荐 HMAC_SHA256。
5. 快手商品新增要求平台图片 URL、类目 ID、SKU、服务规则和运费模板；外网图片需要先通过图片上传接口转成快手平台图片 URL。

### 本次改善

1. 后端新增平台签名工具：
   - 稳定 JSON 序列化。
   - MD5 大写签名。
   - HMAC-SHA256 签名。
   - 按 key 升序拼接参数的签名方法。
2. 拼多多适配器改为官方 `pdd.goods.add` 请求体和 MD5 签名，不再发送泛化 JSON。
3. 抖音商店适配器改为开放平台公共参数签名结构，默认 method 为 `product.add`，可在设置页切换。
4. 快手小店适配器改为 `open.item.new` 系统参数结构，支持 `HMAC_SHA256` 和 `MD5` 签名配置。
5. 设置页新增真实发布所需字段：
   - 拼多多：叶子类目 ID、运费模板 ID、官方网关默认值。
   - 抖音商店：类目 ID、运费模板 ID、接口 Method、官方网关默认值。
   - 快手小店：Sign Secret、类目 ID、运费模板 ID、接口 Method、签名算法、官方网关默认值。
6. 设置检测逻辑改为检查真实发布必填字段，不再只看 Token。
7. 修复 PWA Service Worker 缓存版本和 `/pds-tool/` 路径，避免线上更新后继续加载旧 JS。

### 验证结果

1. `python -m compileall backend/app`：通过。
2. `npm run build`：通过。
3. 生产预览设置页已显示新增字段，包括“叶子类目 ID”“Sign Secret”“运费模板 ID”“接口 Method”和默认官方网关。

## 2026-08-02 11:57 北京时间

### SDK/开放接口复核

1. 拼多多开放平台商品发布链路继续以 `pdd.goods.add` 为核心，前置依赖包括可发布叶子类目、类目属性模板、平台图片和运费模板。
2. 抖店开放平台当前商品发布文档强调 `/product/addV2`，且新版 SKU 库存结构使用 `spec_prices_v2[].stock_num_map`，因此不再把 `product.add` 作为新配置默认值。
3. 快手电商开放平台商品新增接口继续以 `open.item.new` 为核心，签名参数和 `HMAC_SHA256` 默认策略保留。

### 本次改善

1. 抖音商店后端适配器默认 method 从 `product.add` 调整为 `product.addV2`。
2. 抖音商店请求体保留旧版 `spec_prices`，同时新增 `spec_prices_v2`，兼容新版商品发布字段。
3. 后端初始化设置、任务运行配置、前端本地默认配置和设置页提示全部同步为 `product.addV2`。
4. 重新执行前端生产构建，并将最新静态资源同步到 `docs` 发布目录。

### 验证结果

1. `python -m compileall backend/app`：通过。
2. `npm run build`：通过，生成最新前端资源 `index-DbCemib-.js`。

## 2026-08-02 12:18 北京时间

### 邮箱验证码配置检查

1. 邮箱验证码后端闭环已验证：发送验证码接口、验证码入库、邮箱验证码注册、邮箱已验证状态均可跑通。
2. 当前测试环境无法直连外部 SMTP，真实发信测试返回网络不可达；这属于运行环境出网限制，不是邮箱授权码格式错误。
3. 修复后端配置读取路径，固定读取 `backend/.env`，避免因启动目录不同导致 SMTP 配置不生效。
4. 已在本地 `backend/.env` 写入 QQ 邮箱 SMTP 配置；该文件在 `.gitignore` 中，不会提交到 Git。

### 验证结果

1. 程序读取 SMTP 配置：通过，`smtp.qq.com` 和发件邮箱已被识别。
2. `git status --ignored backend/.env backend/app/config.py`：确认 `backend/.env` 被忽略，仅代码配置文件需要提交。

## 2026-08-02 12:34 北京时间

### 登录/注册等待时间优化

1. 前端 API 默认超时从 30 秒缩短为分场景超时：
   - 登录/注册：6 秒。
   - 初始化登录态：3 秒。
   - 邮箱/短信验证码：9 秒。
   - 普通业务请求：12 秒。
2. 登录/注册按钮文案从统一“处理中...”改为“登录中...”或“注册中...”，验证码按钮改为“发送中...”。
3. 邮箱 SMTP 发送改为线程执行并增加 6 秒总超时，避免网络不通时阻塞接口。
4. 邮件发送失败时接口返回明确错误，不再误提示“验证码已发送”。

### 验证结果

1. 邮箱验证码接口在当前 SMTP 网络不通环境下，约 6.21 秒返回 `503` 和“邮件发送超时，请稍后重试或检查 SMTP 网络”。
2. `python -m compileall backend/app`：通过。
3. `npm run build`：通过。
